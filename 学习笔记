1. 关于输入/输出
从计算机组成的角度考虑, 计算机最主要的两个核心功能是 CPU 和 内存。 输入/输出 主要针对的是内存而言的。输入: 就是将外部数据 传给内存。 输出:就是将内存中的数据 拷出去。

2. 设计模式
  2.1 适配器模式
    适配器模式分为两种，1.类适配(继承关系)，2.对象适配(组合关系)。
  2.2 门面模式
    对外提供一个统一的接口(内部封装各个子系统的)，供外部调用(方便，快捷)。

3. 关于c++ 操作符重载
从对象的角度考虑， 两个对象之间会产生很多的关系，搞一些操作符重载，来满足他们的需求。比如算术运算，比较运算，逻辑运算，位运算等等。正是因为这样。

4. 关于c++ dll
  4.1 stdcall cdecall
    stdcall：参数从右向左入参，由被调用接口释放。
    cdecall: 参数从右向左入参，由调用者释放。
  4.2 使用dll 
    共享内存的方式：由调用者申请一块保存数据的内存，然后将这块内存地址传入 到dll 中。 dll 向这块内存填写数据。

